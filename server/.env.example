# ══════════════════════════════════════════════════════
#  Конфигурация сервера
#  Скопируйте этот файл в .env и заполните значения
# ══════════════════════════════════════════════════════

# Порт сервера (по умолчанию 3000)
PORT=3000

# Токен бота из MAX Developer Portal (dev.max.ru)
# Получить: dev.max.ru → Чат-боты → Ваш бот → Токен
BOT_TOKEN=

# Токен бота менеджеров аренды.
# Если все роли работают через один MAX-бот, можно оставить пустым — будет использован BOT_TOKEN.
MANAGER_BOT_TOKEN=

# Токен доставочного бота.
# Если все роли работают через один MAX-бот, можно оставить пустым — будет использован BOT_TOKEN.
DELIVERY_BOT_TOKEN=

# Публичный URL для webhook или внешнего доступа к серверу
# Пример для Railway:
# WEBHOOK_URL=https://your-service.up.railway.app
WEBHOOK_URL=

# Отдельный публичный URL для доставочного webhook (необязательно).
# Если не указан, будет использован WEBHOOK_URL.
DELIVERY_WEBHOOK_URL=

# Отдельный публичный URL для webhook бота менеджеров (необязательно).
# Если не указан, будет использован WEBHOOK_URL.
MANAGER_WEBHOOK_URL=

# Резервное получение событий MAX через /updates.
# Оставьте включённым, если webhook в MAX иногда не доставляет сообщения.
MAX_POLLING_ENABLED=1
MAX_POLL_INTERVAL_MS=5000
